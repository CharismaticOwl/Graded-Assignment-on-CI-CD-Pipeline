pipeline{
    agent any
    stages{
        stage('clone the repo'){
            steps{
                git branch: 'Jenkins',
                    url: 'https://github.com/CharismaticOwl/Graded-Assignment-on-CI-CD-Pipeline.git'
            }
        }
    }
}